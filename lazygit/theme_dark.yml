git:
  # 全体構成: diffRenderersは配列で複数のdiff表示方法を定義でき、diff画面で `|` (cycleDiffRenderers)
  #           を押すとリストを順に切り替えられる。ここでは「上下分割(unified)」と「左右分割(side-by-side)」を
  #           トグルできるよう2つ用意している。
  # 補足: 旧名はgit.pagers(配列内のキーはpager)。lazygitのバージョンアップでリネームされており、
  #       旧名のまま起動すると自動マイグレーションで設定ファイルが書き換えられるため、新しい名前で記述する。
  diffRenderers:
    # 1つ目(デフォルト): unified表示。1ファイル内で変更行を上下に並べて比較する。
    - colorArg: always
      command: delta --paging=never --syntax-theme="ansi"
    # 2つ目: side-by-side表示。--side-by-side で旧/新を左右2カラムに分割する。
    #         `|` キーで1つ目との間をトグルする。
    - colorArg: always
      command: delta --paging=never --side-by-side --syntax-theme="ansi"
gui:
  theme:
    activeBorderColor:
      - '#a7c080'  # 緑系（フォレストの葉）
      - bold
    inactiveBorderColor:
      - '#7a8478'  # グレイッシュグリーン
    optionsTextColor:
      - '#7fbbb3'  # アクアブルー
    selectedLineBgColor:
      - '#3d484d'  # 暗めの背景（選択時）
    cherryPickedCommitBgColor:
      - '#475258'  # やや明るいグレー
    cherryPickedCommitFgColor:
      - '#a7c080'  # 緑系
    unstagedChangesColor:
      - '#e67e80'  # 柔らかい赤（エラー/変更）
    defaultFgColor:
      - '#d3c6aa'  # クリーム系の文字色
    searchingActiveBorderColor:
      - '#dbbc7f'  # 黄色系（検索時の強調）

  authorColors:
    '*': '#7fbbb3'  # アクアブルー
 
#gui:
#  theme:
#    activeBorderColor:
#      - '#8caaee'
#      - bold
#    inactiveBorderColor:
#      - '#a5adce'
#    optionsTextColor:
#      - '#8caaee'
#    selectedLineBgColor:
#      - '#414559'
#    cherryPickedCommitBgColor:
#      - '#51576d'
#    cherryPickedCommitFgColor:
#      - '#8caaee'
#    unstagedChangesColor:
#      - '#e78284'
#    defaultFgColor:
#      - '#c6d0f5'
#    searchingActiveBorderColor:
#      - '#e5c890'
#
#  authorColors:
#    '*': '#babbf1'
