# tmux起動時のシェルをzshにする
set-option -g default-shell /bin/zsh

# tmux内のアプリケーションからエスケープシーケンスを外側のターミナルへ透過させる
# Claude Codeのプログレスバーやターミナルネイティブ通知に必要
set -g allow-passthrough on

# tmuxを256色表示できるようにする
set-option -g default-terminal tmux-256color
set -g terminal-overrides 'xterm:colors=256'

# 全体: CSI u エンコーディングを有効化し、Tab と Ctrl-i 等の修飾キーを区別可能にする
# 背景: 従来のターミナルでは Tab と Ctrl-i は同じバイト(0x09)を送信するため区別できない
#       CSI u エンコーディングにより、修飾キーを個別のシーケンスとして送信可能になる
# 構成: 以下の2つの設定が両方揃って初めて、Ghostty → tmux → Neovim のキーシーケンス伝達が成立する
#   extended-keys: tmux内のアプリケーション(Neovim等)が拡張キーを要求した際に、tmuxがそれに応答する
#   terminal-features: 外側のターミナル(Ghostty)が拡張キーに対応していることをtmuxに教える
#     tmuxは外側ターミナルの全機能を自動検出できないため、extkeysは明示的な宣言が必要
#     'xterm-ghostty'はGhosttyのTERM名で、tmux display -p '#{client_termname}' で確認可能
set -s extended-keys on
set -as terminal-features 'xterm-ghostty:extkeys'

# prefixキーをC-tに変更
#set -g prefix C-t
set -g prefix C-s

# C-bのキーバインドを解除
unbind C-b

# karabinerのspace + a,sでの移動に対応させるためにkeymapを定義
bind -n M-p previous-window
bind -n M-n next-window

# デフォルトはステータスバーを非表示にする
set-option -g status off
# キーバインド設定 (例: Prefix + b で表示/非表示を切り替え)
bind-key b set-option status
# ステータスバーをトップに配置する
set-option -g status-position top
#set-option -g status-position bottom
set -g renumber-windows on
# 現状のwindowの位置を移動する
bind-key > run-shell 'wid=$(tmux display-message -p "#{window_id}"); tmux swap-window -t +1; tmux select-window -t "$wid"'
bind-key < run-shell 'wid=$(tmux display-message -p "#{window_id}"); tmux swap-window -t -1; tmux select-window -t "$wid"'
# 左右のステータスバーの長さを決定する
#set-option -g status-left-length 90
#set-option -g status-right-length 90
# 左右の領域を広く確保（左右で画面を二分するイメージ）
# 端末幅より十分大きくしておくと、実質「左半分」「右半分」を占有できます
#set -g status-left-length 100
#set -g status-right-length 100

set -g status-left-length 80
set -g status-right-length 120  # 右に十分な幅を確保
# 左: ウィンドウリストを左詰めで表示
# 現在のwindow名の反転スタイルはそのまま活かす
# window-status-format / window-status-current-format は既存設定を使用
# 左側の固定テキスト（[1]など）を削除して、プレフィックスは空にする
set -g status-left ""

# 右: ブランチ名 + 日付（シンプルに可変でもOK。幅確保で中央は動かない）
#set -g status-right '#(git -C "#{pane_current_path}" symbolic-ref --quiet --short HEAD 2>/dev/null || echo "") %Y-%m-%d(%a) %H:%M'
# パディングで中央寄せ風に（スペース数は端末幅に合わせて微調整）
#set -g status-right '          #(git -C "#{pane_current_path}" symbolic-ref --quiet --short HEAD 2>/dev/null || echo "")  %Y-%m-%d(%a) %H:%M'
# 変更前のversion
set -g status-right '#[reverse] #((git -C "#{pane_current_path}" symbolic-ref --quiet --short HEAD 2>/dev/null || echo "") | cut -c1-15) #[default]%Y-%m-%d(%a) %H:%M'
# 変更後のversion。CPU使用率・Load Average・gitブランチを表示（時刻は非表示）
# top -l 1 -s 0: 1回だけサンプリング、0秒遅延で即時取得（約1秒かかるが15秒間隔なら負荷は無視できる）
# LA形式: Load Average/CPUコア数（例: 2.58/10）- LAがコア数を超えると高負荷状態
set -g status-right 'CPU:#(top -l 1 -s 0 | grep "CPU usage" | awk "{print \$3}") LA:#(sysctl -n vm.loadavg | awk "{print \$2}")/#(sysctl -n hw.ncpu) #[reverse] #((git -C "#{pane_current_path}" symbolic-ref --quiet --short HEAD 2>/dev/null || echo "") | cut -c1-15) '
#set -g @git_branch '#(git -C "#{pane_current_path}" symbolic-ref --quiet --short HEAD 2>/dev/null || echo "")'
#set -g status-right '#[reverse] #{E:@git_branch} #[default]%Y-%m-%d(%a) %H:%M'

# 非アクティブはデフォルトのままでもOK。必要なら次のように:
# set -g window-status-format " #I:#W "
# #P => ペイン番号
# 最左に表示
# hostとprocessを表示
#set-option -g status-left '#H:[#P]'

# Wi-Fi、バッテリー残量、現在時刻
# Wi-Fi、バッテリー残量が表示されないから下の設定をコメントアウト
# 最右に表示
# set-option -g status-right "#(wifi) #(battery --tmux)[LA: #(uptime | awk -F'load averages: ' '{print $2}')] [%Y-%m-%d(%a) %H:%M]"
# set-option -g status-right "LA: #(uptime | awk -F'load averages: ' '{print $2}') | %Y-%m-%d(%a) %H:%M"

# ステータスバーを15秒毎に描画し直す（topコマンドの負荷軽減のため）
set-option -g status-interval 15


# ウィンドウリストを左寄せ（中央寄せはやめる）
set -g status-justify left
# センタライズ（主にウィンドウ番号など）
#set-option -g status-justify centre

# ステータスバーの色を設定する
#set-option -g status-bg "colour251"
#set-option -g status-bg "colour153"
#set-option -g status-bg "colour238"
set-option -g status-bg "colour247"

# status line の文字色を指定する。
set-option -g status-fg "colour232"

# アクティブなウィンドウを目立たせる
#setw -g window-status-current-style fg=white,bg=blue,bright
# ウィンドウ名の表示設定
# gitリポジトリ内ならブランチ名、そうでなければデフォルトのウィンドウ名を表示
# 非アクティブウィンドウ（ブランチ名は最大15文字に制限）
# 注意: tmux設定ファイル内では$をエスケープ(\$)、%はtmuxフォーマット文字のためcutで文字数制限
#   判定方法: window_name が現在ペインのプロセス名 (pane_current_command) と一致する場合は
#   automatic-rename による自動命名とみなしブランチ名を表示する。
#   rename-window で手動命名すると window_name はプロセス名と一致しなくなるため #W をそのまま表示する。
#   ※ #{automatic_rename} はオプションが未設定の window で空文字列を返し条件式で常に false になるため使わない
set -g window-status-format " #I:#{?#{==:#{window_name},#{pane_current_command}},#(cd #{pane_current_path}; b=\$(git symbolic-ref --quiet --short HEAD 2>/dev/null); [ -n \"\$b\" ] && echo \"\$b\" | cut -c1-15 || echo '#W'),#W} "
# アクティブウィンドウ（反転表示、ブランチ名は最大15文字に制限）
set -g window-status-current-format "#[reverse] #I:#{?#{==:#{window_name},#{pane_current_command}},#(cd #{pane_current_path}; b=\$(git symbolic-ref --quiet --short HEAD 2>/dev/null); [ -n \"\$b\" ] && echo \"\$b\" | cut -c1-15 || echo '#W'),#W} "
bind Space last-window    # prefix +   Space で直前のウィンドウへ
# vimのキーバインドでペインを移動する
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# vimのキーバインドでペインをリサイズする
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# | でペインを縦分割する
bind | split-window -h

# - でペインを縦分割する
bind - split-window -v

# 番号基準値を変更
set-option -g base-index 1

# マウス操作を有効にする
set-option -g mouse on
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"

# コピーモードを設定する
# コピーモードでvimキーバインドを使う
setw -g mode-keys vi

# Vimのvisual modeに対応: prefix + v でコピーモードに入る
# デフォルトの prefix + [ も維持する（unbindしない）
bind-key v copy-mode

# コピーモード中の選択範囲ハイライト色（Catppuccin Frappeのpeach）
set -g mode-style "fg=black,bg=#fab387"

# コピーモード進入/退出時にカーソル色を変更して、モードを視覚的に識別する
# tmux 3.3+の組み込みcursor-colourオプションを使用（エスケープシーケンス不要）
# -p: ペイン単位で設定し、他のペインに影響しない
set-hook -g pane-mode-changed \
  'if-shell -F "#{pane_in_mode}" \
    "set -p cursor-colour orange" \
    "set -pu cursor-colour"'

# 'v' で選択を始める
bind -T copy-mode-vi v send -X begin-selection

# 'V' で行選択
bind -T copy-mode-vi V send -X select-line

# 'C-v' で矩形選択
bind -T copy-mode-vi C-v send -X rectangle-toggle

# 'y' でヤンク
bind -T copy-mode-vi y send -X copy-selection

# 'Y' で行ヤンク
bind -T copy-mode-vi Y send -X copy-line

# 'C-p'でペースト
bind-key C-p paste-buffer

# fzfによるウィンドウ検索・選択 (prefix + g)
# 表示テキスト全体（ブランチ名・パス含む）をインクリメンタル検索可能
bind-key g display-popup -E "tmux list-windows -F '#{p2:window_index} │ #{p20:#{=20:#{?#{==:#{window_name},#{pane_current_command}},#(cd #{pane_current_path}; b=\$(git symbolic-ref --quiet --short HEAD 2>/dev/null); [ -n \"\$b\" ] && echo \"\$b\" || echo '#W'),#{window_name}}}} │ #{pane_current_path}' | sed \"s|$HOME|~|\" | fzf | cut -d'│' -f1 | tr -d ' ' | xargs -I{} tmux select-window -t {}"

# choose-treeによるウィンドウ選択 (prefix + e)
# プレビュー付きのツリー表示で、現在のセッションのウィンドウのみ表示
bind-key e choose-tree -Zw -f '#{session_attached}' -F '#(printf "%-6s │ %-42s │ %s" "#{window_index}:#{window_name}" "$(git -C "#{pane_current_path}" symbolic-ref --quiet --short HEAD 2>/dev/null || echo "-")" "#{pane_current_path}" | sed "s|$HOME|~|")'

# AIコーディングアシスタント3面分割 (prefix + a)
# 全体構成: 新規windowを作成し、3ペインに分割してclaude/codex/geminiを起動する
# 手順: 新規window → claude起動 → 右にcodex → 右下にgemini → フォーカスを左に戻す
# 最後にsynchronize-panesを有効化し、全ペインに同時入力可能にする
bind-key a \
  new-window \; \
  send-keys 'claude' Enter \; \
  split-window -h -l 50% \; \
  send-keys 'codex' Enter \; \
  split-window -v -l 50% \; \
  send-keys 'gemini' Enter \; \
  select-pane -t 0 \; \
  set-option synchronize-panes on

# 全ペインへの同時入力をトグルする (prefix + S)
# prefix + a で有効化した後、一部ペインだけに入力したい場合にoff/onを切り替える
bind-key S set-option synchronize-panes

# issue連動worktree作成 + nvim + ClaudeCode起動 (prefix + i)
# 全体構成: 新windowを作り、gwc関数でworktree作成→nvim起動→ClaudeCodeに /check-issue を送信する
# 手順: 現在ペインのcwdを引き継いで新windowを作成し、interactive zshでgwcを実行する
# zsh -ic: .zshrcをロードしてgwc関数とghdev関数を使える状態にする
bind-key i new-window -c "#{pane_current_path}" "zsh -ic gwc"

# dotfilesディレクトリをNeovimで開く (prefix + d)
# tmux popupで独立したNeovimセッションをオーバーレイ表示する
# -E: nvim終了時にpopupを自動で閉じる
# -w/-h: 画面の80%サイズで表示（Snacks.nvimのフローティングウィンドウと同等）
# -B: popup内のC-w等のデフォルトキーバインドを無効化し、nvimにそのまま渡す
bind-key f display-popup -w 80% -h 80% -B -E "cd ~/settings/dotfiles && nvim ."

# terminalのfocusの変更を、tmuxからcluent(neovimなど)に渡す
set -g focus-events on

# --------
# plugins
# --------

# List of plugins
# --essentials
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# --essentials

# other plugins
set -g @plugin 'sainnhe/tmux-fzf'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
#run '~/.tmux/plugins/tpm/tpm'
# change default installation method with homebrew
run '$(brew --prefix tpm)/share/tpm/tpm'

TMUX_FZF_ORDER="window|pane|command|keybinding|clipboard|process|session"
